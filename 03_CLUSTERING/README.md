# Sequence Clustering Outputs

This directory contains the results of sequence clustering to establish non-redundant viral operational taxonomic units (vOTUs).

These outputs are excluded from version control.
